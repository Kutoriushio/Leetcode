# Solution(Python)

## Solution1 (TC (O(N)) SC (O(N)))

Use level order traversal, the same sa [048](048.%20Populating%20Next%20Right%20Pointers%20in%20Each%20Node/README.md)

## Solution2 (TC (O(N)) SC (O(1)))

1. Each level can be seen as a linked list.